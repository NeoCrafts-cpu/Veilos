# Architecture

The canonical architecture document is the repository-root [`ARCHITECTURE.md`](../ARCHITECTURE.md).

This file exists so the layout in `VELIOS_CURSOR_MASTER_PLAN.md` (`docs/architecture.md`) has a stable path for judges and agents.
