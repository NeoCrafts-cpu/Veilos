# `@velios/policy-engine`

Local, non-authoritative helpers for encoding commitments and previewing predicates.

The Compact circuit is the authority. This package must never be treated as proof that an action was authorized.
